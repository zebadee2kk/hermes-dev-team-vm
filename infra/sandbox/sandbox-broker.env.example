# Install as /etc/forge/sandbox-broker.env with mode 0600.
FORGE_SANDBOX_WORKSPACE_ROOT=/var/lib/forge/workspaces
FORGE_SANDBOX_EXECUTION_ENABLED=true
# Generate a high-entropy value; never commit the real broker credential.
FORGE_SANDBOX_BROKER_KEY=replace-me
